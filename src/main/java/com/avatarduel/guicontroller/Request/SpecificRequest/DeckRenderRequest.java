package com.avatarduel.guicontroller.Request.SpecificRequest;

import com.avatarduel.model.type.PlayerType;

public class DeckRenderRequest extends SpecificPlayerRequest {
    public DeckRenderRequest(PlayerType playerType) {
        super(playerType);
    }
}
